Two 4-bit input data are multiplied together and added with the value of a 4-bit accumulator register. The added value is then stored in the accumulator register. Both the inputs are sent as outputs. The value stored in the accumulator register is sent to another output of 4-bits.

