root ::= inputs outputs "\n"

identifier ::= [a-zA-Z] [a-zA-Z0-9]*
naturalNum ::= [1-9] [0-9]*

width ::= "width:" naturalNum

port ::= identifier "{" width "}"
inputs ::= "inputs{" port+ "}"
outputs ::= "outputs{" port+ "}"

